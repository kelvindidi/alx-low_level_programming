#include "main.h"
#include <stdio.h>

/**
 * _puts - Print a string followed by a newline character.
 * @str: The string to print.
 *
 * Description: This function prints the string
 * 'str' followed by a newline character to stdout.
 */
void _puts(char *str)
{
	puts(str);
}
