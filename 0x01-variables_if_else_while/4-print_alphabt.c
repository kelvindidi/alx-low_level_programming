#include <stdio.h>
/*
* mian - this is the main function
* Returna:0
*/
int main(void)
{
	char lowerCase;
	char upperCase;
	lowerCase = 'a';
	upperCase = 'A';
	while (lowerCase < 'z')
	{
		if (lowerCase == 'q' || lowerCase == 'e'))
		{
			continue;
		}
		putchar(lowerCase);
