#include <string.h>
#include "main.h"

/**
* _strcmp - compares teo strings
* @s1: string one
* @s2: string two
* Return: 1 if they are the same, else 0
*/

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
